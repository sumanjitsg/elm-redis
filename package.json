{
  "name": "@sumanjitsg/redistil-elm",
  "description": "A minimalist Redis implementation in Elm, bringing the power of functional programming to Redis interactions.",
  "version": "0.1.0",
  "main": "dist/index.js",
  "bin": {
    "redistil-elm-server": "dist/index.js"
  },
  "files": [
    "dist/*.js"
  ],
  "keywords": [
    "elm",
    "redis",
    "database",
    "functional programming"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sumanjitsg/redistil-elm.git"
  },
  "author": "Sumanjit Sengupta",
  "license": "GPL-3.0-only",
  "scripts": {
    "test": "npx elm-test",
    "build": "npx elm make src/Main.elm --optimize --output=dist/elm.js && npx tsc -p tsconfig.json",
    "dev": "npx elm make src/Main.elm --output=dist/elm.js && npx tsc -p tsconfig.json",
    "format": "elm-format src/ --yes",
    "publish": "npm publish --access public",
    "prepublishOnly": "npm run test && npm run build"
  },
  "devDependencies": {
    "@types/elm": "^0.19.3",
    "@types/node": "^20.11.19",
    "@types/uuid": "^9.0.8",
    "elm": "^0.19.1-6",
    "elm-format": "^0.8.7",
    "elm-test": "^0.19.1-revision12",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "uuid": "^9.0.1"
  }
}
